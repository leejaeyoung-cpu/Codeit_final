# 배경 제거 API - 2026년 1월 2일 업데이트

## 🎉 완성된 기능

### 1. AI 배경 제거
- ✅ rembg 라이브러리를 활용한 고품질 배경 제거
- ✅ 투명 배경 PNG 출력
- ✅ 선택적 배경색 추가 (JPEG)
- ✅ GPU/CPU 자동 선택

### 2. 인스타그램 비율 지원
- ✅ **4:5 비율 (1080x1350px)** - 세로형 포스트
- ✅ 1:1 비율 (1080x1080px) - 정사각형
- ✅ 16:9 비율 (1080x566px) - 가로형

### 3. FastAPI REST API
- ✅ `POST /api/v1/remove-background` - 배경 제거 메인 API
- ✅ `POST /api/v1/image-info` - 이미지 정보 조회
- ✅ `GET /api/v1/health` - 헬스 체크
- ✅ Swagger UI 문서 (`/docs`)
- ✅ CORS 설정 완료

### 4. 웹 UI
- ✅ 간단하고 직관적인 인터페이스
- ✅ 드래그 앤 드롭 이미지 업로드
- ✅ 실시간 결과 미리보기
- ✅ 비율 선택 드롭다운
- ✅ 배경색 커스터마이징
- ✅ 원클릭 다운로드

---

## 🚀 빠른 시작

### 1. 설치
```bash
# 저장소 클론
git clone https://github.com/leejaeyoung-cpu/Codeit_final.git
cd Codeit_final

# 가상환경 생성 및 활성화
python -m venv venv
venv\Scripts\activate  # Windows

# 의존성 설치
pip install -r requirements.txt
```

### 2. 서버 실행
```bash
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

### 3. 웹 UI 접속
브라우저에서 http://127.0.0.1:8000 열기

---

## 📸 스크린샷

### 웹 UI
![웹 UI](docs/screenshots/web_ui.png)

### 처리 결과
![처리 결과](docs/screenshots/result_comparison.png)

---

## 💻 API 사용 예제

### Python
```python
import requests

url = "http://127.0.0.1:8000/api/v1/remove-background"
files = {'file': open('product.jpg', 'rb')}
data = {'ratio': '4:5'}

response = requests.post(url, files=files, data=data)

with open('result.png', 'wb') as f:
    f.write(response.content)
```

### cURL
```bash
curl -X POST "http://127.0.0.1:8000/api/v1/remove-background" \
  -F "file=@product.jpg" \
  -F "ratio=4:5" \
  --output result.png
```

---

## 📊 성능

| 항목 | 결과 | 목표 | 상태 |
|------|------|------|------|
| 처리 시간 (CPU) | ~5초 | < 10초 | ✅ |
| 처리 시간 (GPU) | ~2초 | < 5초 | ✅ |
| 파일 크기 | 80KB | < 500KB | ✅ |
| 정확도 | 높음 | 95%+ | ✅ |

---

## 🛠️ 기술 스택

### 백엔드
- **Python 3.11+**
- **FastAPI** - REST API 프레임워크
- **rembg** - AI 배경 제거
- **Pillow** - 이미지 처리
- **Uvicorn** - ASGI 서버

### AI/ML
- **PyTorch** - 딥러닝 프레임워크
- **ONNX Runtime** - 모델 실행
- **u2net** - 배경 분리 모델

### 프론트엔드
- **HTML5 + CSS3**
- **Vanilla JavaScript**
- **Fetch API**

---

## 📁 프로젝트 구조

```
Codeit_final/
├── app/
│   ├── main.py                    # FastAPI 앱 + 웹 UI 서빙
│   ├── core/
│   │   ├── background_removal.py  # 배경 제거 서비스
│   │   └── image_processing.py    # 이미지 처리 유틸
│   └── api/
│       └── v1/
│           └── image.py           # API 엔드포인트
├── static/
│   └── index.html                 # 웹 UI
├── tests/
│   └── test_background_removal.py # 테스트 코드
├── scripts/
│   ├── test_rembg.py              # 성능 검증 스크립트
│   └── validate_performance.py    # 벤치마크
├── requirements.txt               # Python 의존성
└── README.md
```

---

## 🔧 개발된 기능 상세

### 배경 제거 서비스
- 자동 배경 분리
- 알파 채널 생성
- 에지 스무딩
- 배치 처리 지원

### 이미지 처리
- 인스타그램 비율 리사이징
- 비율 유지 리사이징
- 배경색 추가
- PNG/JPEG 변환

### API 기능
- 멀티파트 파일 업로드
- 비율 파라미터 (4:5, 1:1, 16:9)
- 배경색 옵션 (hex 색상)
- 처리 시간 헤더 응답

---

## 🧪 테스트

### 단위 테스트
```bash
pytest tests/ -v
```

### API 테스트
```bash
python scripts/test_rembg.py <image_path>
```

### 성능 벤치마크
```bash
python scripts/validate_performance.py <image_path>
```

---

## 📖 API 문서

서버 실행 후 다음 URL에서 Swagger UI 확인:
- http://127.0.0.1:8000/docs
- http://127.0.0.1:8000/redoc

---

## 🎯 향후 계획

### Week 2 (예정)
- [ ] RMBG-2.0 모델 업그레이드
- [ ] GPU 최적화
- [ ] Redis 캐싱
- [ ] 배치 처리 API

### Week 3 (예정)
- [ ] Google Cloud Storage 연동
- [ ] 이미지 품질 향상 필터
- [ ] 배경 생성 기능

---

## 📝 변경 로그

### 2026-01-02
- ✅ rembg 기반 배경 제거 구현
- ✅ 4:5 비율 인스타그램 포맷 지원
- ✅ FastAPI REST API 완성
- ✅ 웹 UI 구현
- ✅ API 검증 및 테스트 완료

### 2025-12-30
- ✅ 프로젝트 초기 설정
- ✅ GitHub 저장소 생성

---

## 👨‍💻 개발자

**이재영** - 이미지 처리 모듈 개발자
- GitHub: [@leejaeyoung-cpu](https://github.com/leejaeyoung-cpu)
- 프로젝트: [Codeit_final](https://github.com/leejaeyoung-cpu/Codeit_final)

---

## 📄 라이선스

MIT License - 자세한 내용은 [LICENSE](LICENSE) 파일 참조

---

## 🙏 감사의 말

- **rembg 팀** - 훌륭한 배경 제거 라이브러리 제공
- **FastAPI 팀** - 현대적인 API 프레임워크
- **AdGen_AI 팀** - 프로젝트 기회 제공

---

**⭐ 이 프로젝트가 도움이 되셨다면 Star를 눌러주세요!**
